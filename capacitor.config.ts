import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'jean.tectronix.cl',
  appName: 'prueba',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
